# PROGRESS

## Completed
- Reviewed `AGENTS.md` and all copied skill files under `.scratch/skills/`.
- Rewrote `AGENTS.md` so repo operational guidance matches `pyagent`.
- Normalized Python tooling skills (`devenv`, Ruff, Ty) to repo-local commands.
- Replaced copied Remora/Browsee-specific skills with pyagent-aligned templates and explicit "not scaffolded yet" status where appropriate.

## Pending
- If/when `src/`, `tests/`, browser harness, or `e2e/` are scaffolded, refine skills from template mode to concrete workflow mode.
